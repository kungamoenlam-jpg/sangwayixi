# Local environment (Supabase keys, DATABASE_URL, etc.)
.env

# Test run artifacts
tests/tmp-data/

# Cached Azure TTS audio — regenerated on demand, not source content.
data/tts-cache/

# Own-voice recordings made via /record.html now live in Supabase Storage, not
# this repo — nothing under audio/ should ever be committed.
audio/
